

CREATE TABLE messages (
messageId int,
navigationalStatus text,
utcSecond text,
extension text,
heading text,
speedOverGround text,
utcDay text,
utcMonth text,
actorType text,
mmsi text,
name text,
imo text,
utcMinute text,
slots text,
callSign text,
longitude int,
messageSequenceNumber text,
utcYear text,
timeStamp text,
latitude int,
destination text,
unit text,
messageIncrement text,
messagePayload text,
specialManoeuvre text,
typeLabel text,
utcHour text,
courseOverGround text,
acknowledgementFo text,
utcReport text,
messageDestination text,
messageOffset text,
position text,
messageApplicationID text,
dimensions text,
rateOfTurn text,
timeOut text,
eta text);